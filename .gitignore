# Private reference images (real medical/forensic photos): never commit
refs/
# Throwaway test scripts left by build agents in the repo root
/t_*.py
# Full-body working .blend (~70 MB, regenerated by blender/gore_body/build.py; the game uses the exported .glb files)
blender/gore_body/gore_body.blend
*.blend1
