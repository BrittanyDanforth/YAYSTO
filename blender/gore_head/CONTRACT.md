# Gore Head: build contract

Every module in this folder follows this contract so the parts fit together.
**Hard rule: everything is generated by code.** No downloaded meshes, images,
HDRIs, textures, add-ons or pip packages besides `bpy` itself. Procedural
geometry and procedural shader nodes only.

Target: Blender 5.x. Scripts must run both as `python3 build.py` (the `bpy`
module, which is what is installed here, version 5.0.1) and as
`blender -b --python build.py` on the user's Blender 5.1. Use only paths relative
to this folder (`gh_common.HERE`), never absolute paths.

## Files

| File | Owner | Entry point |
|---|---|---|
| `gh_common.py` | lead (done) | scene reset, collections, `ensure_controls()`, `drive()`, stage lighting, `render_views()` |
| `anatomy.py` | anatomy builder | `build_anatomy() -> dict[str, bpy.types.Object]` |
| `materials.py` | materials builder | `build_materials() -> dict[str, Material]`, `assign_materials(objs: dict, mats: dict)` |
| `gore.py` | gore builder | `build_gore_system(objs: dict) -> dict`, `add_hit(kind, location, ...)`, `clear_hits()` |
| `build.py` | integrator | assembles all, presets, renders, saves `gore_head.blend` |
| `renders/` | all | PNG outputs (module test renders use a module prefix, e.g. `anatomy_*.png`) |

Each module must also be runnable on its own (`python3 anatomy.py`) to produce
its own test renders. Keep test renders small (≤ 640 px, ≤ 48 samples,
denoised); the machine has 4 CPU cores and no GPU, and other builders render
at the same time.

## Space and units

- Meters. Z up. **Face looks toward −Y.** Character's left = +X (`_L` suffix), right = −X (`_R`).
- Origin (0,0,0) = midpoint between the ear canals.
- Real adult proportions. Landmarks (anatomy builder may adjust by a few mm and
  must then update this table):

| Landmark | Position (m) |
|---|---|
| Top of head (vertex) | (0, 0.005, 0.125) |
| Back of head | (0, 0.100, 0.030) |
| Glabella (between brows) | (0, −0.093, 0.035) |
| Eyeball centers | (±0.032, −0.070, 0.022), radius 0.012 |
| Nose tip | (0, −0.111, −0.014) |
| Mouth center (between lips) | (0, −0.094, −0.055), lips parted 6.9 mm, mouth width ≈ 0.048 (corners ±0.024, y −0.085) |
| Chin bottom | (0, −0.080, −0.103) (pogonion, most forward chin point: (0, −0.094, −0.092)) |
| Ear canals | (±0.072, 0, 0) |
| Head half-width | ≈ 0.074 |
| Neck | ≈ radius 0.055, center y ≈ +0.015, cut flat at z = −0.20 |
| Nasion / subnasale | (0, −0.089, 0.023) / (0, −0.097, −0.030) |
| Upper / lower incisal edge | z = −0.0544 / −0.0569 (upper incisor labial face y ≈ −0.088) |

`anatomy.ANATOMY_LANDMARKS` holds the values measured on the built geometry
(chin and nose base were moved by 3-4 mm from the first draft of this table
to get realistic facial thirds).

## Objects (all in collection `GoreHead`)

Layers from outside in. Every one is a real, closed or properly bounded mesh
with smooth shading and enough density for wound displacement (skin ≥ ~60k
evaluated verts; others as needed).

| Object | What it is |
|---|---|
| `GH_Skin` | outer skin of head + neck. Eye openings with eyelids, nose with nostrils, ears, **lips slightly parted (≈6-8 mm)** leading into a mouth cavity so teeth and tongue are visible |
| `GH_Muscle` | soft tissue shell between skin and skull (≈3-4 mm under skin on the scalp, thicker on cheeks/jaw). Seen only through wounds |
| `GH_Skull` | cranium + face bones with **real bone thickness** (outer + inner surface, ≈6-7 mm), orbits (eye sockets), nasal opening, cheekbones, upper jaw |
| `GH_Jaw` | mandible |
| `GH_Brain` | two hemispheres with gyri/sulci folds, cerebellum, brain stem, fits inside skull cavity with ≈2-3 mm gap |
| `GH_Eye_L`, `GH_Eye_R` | eyeballs (sclera, iris, pupil, clear cornea bulge), local −Y looks forward, seated in orbits behind eyelids |
| `GH_Teeth_Upper`, `GH_Teeth_Lower` | full rows of individual teeth (incisors, canines, premolars, molars) set in `GH_Gums` |
| `GH_Gums` | upper and lower gums |
| `GH_Tongue` | tongue in mouth cavity |
| `GH_MouthCavity` | inner mouth lining (may be part of `GH_Skin` instead, then omit) |
| `GH_Controls` | empty with global gore sliders (created by `gh_common.ensure_controls()`) |
| `GH_Eyebrows`, `GH_Eyelashes` | hair curves (materials `GH_Hair`, `GH_Hair_Lash`) grown by `build.py`; a small GN modifier (`GH_HairOnSkin`) keeps them on the wounded skin and removes strands whose root is burned or torn open |

`build_anatomy()` returns a dict keyed by the object names above.

## Gore system (owned by `gore.py`)

### Placing wounds

One collection per wound type, under a parent collection `GH_Hits`:

| Collection | Wound |
|---|---|
| `GH_Hits_Bullet` | bullet entry: small round hole, abrasion ring, punches through every layer when deep |
| `GH_Hits_Exit` | exit wound: large, ragged, torn outward (everted) flesh, bone fragments |
| `GH_Hits_Slash` | cut / laceration: long thin gash along the empty's local X axis, gaping edges |
| `GH_Hits_Blunt` | blunt trauma: bruise, swelling, split skin, depressed skull fracture when deep, knocks out nearby teeth |
| `GH_Hits_Burn` | burn: charred, blistered, peeling skin |

Each hit is an **empty** in one of those collections, so the user can drag,
rotate and scale it in the viewport and the wound updates live:

- location = impact point (anywhere near the surface; the system finds the surface)
- rotation: local −Z = direction the damage travels into the head; local X = slash direction
- scale.x = size (1.0 = default size for that wound type)
- scale.y = elongation (1.0 = round; slash length comes from this)
- scale.z = depth (0.3 = skin only, 0.6 = to the bone, 1.0 = through the skull into the brain)

`add_hit(kind, location, direction=None, size=1.0, elongation=1.0, depth=0.6, name=None)`
creates such an empty (`kind` in `bullet|exit|slash|blunt|burn`), and `clear_hits()`
removes all of them.

### Modifiers

A geometry-nodes modifier named **`GH_Gore`** on every damageable layer
(`GH_Skin`, `GH_Muscle`, `GH_Skull`, `GH_Jaw`, `GH_Brain`, eyes, teeth, gums, and
`GH_MouthCavity` / `GH_Tongue` as soft tissue so wounds through the mouth open them too). All use one
shared node group (plus internal subgroups) with a layer setting, so wounds in
nested layers line up: the skin hole is widest, muscle smaller, skull smaller
still and only opened when depth is enough, brain gets a crater. Hole edges are
ragged (noise), wound walls have thickness so you see the layers, not paper-thin
shells. Global controls reach the modifiers through drivers from `GH_Controls`
(use `gh_common.drive`).

Blood drips: generated by the skin layer as real geometry (tubes/beads that
start at wounds, run down the surface with gravity, follow the surface, thicken
at the tip, length grows with `drip_time` and `bleed`). Material `GH_Blood`.

### Attributes written for shaders (float point attributes, 0..1)

| Attribute | Meaning |
|---|---|
| `gore_wound` | 1 inside an open wound (exposed tissue), 0 on intact surface |
| `gore_depth` | depth through the layers inside a wound: 0 = skin surface, ~0.3 fat, ~0.6 muscle, 1 = bone. Used to paint skin → fat → muscle rings on wound walls |
| `gore_edge` | torn edge / abrasion rim around a wound |
| `gore_blood` | blood coverage on the surface (pooled around wounds, smears, spatter) |
| `gore_bruise` | bruise strength |
| `gore_burn` | burn / char strength |
| `gore_fracture` | skull & jaw: cracked bone around a breach |

Missing attributes read as 0, so every material must look correct with no gore at all.

### Global controls (`GH_Controls` custom properties, all 0..1)

`damage`, `bleed`, `drip_time`, `wetness`, `blood_age`, `bruising`, `swelling`,
`skin_tone`, `pallor`, as defined in `gh_common.CONTROL_PROPS`. Materials drive
their own node values from `wetness`, `blood_age`, `skin_tone`, `pallor`; the GN
modifiers drive from `damage`, `bleed`, `drip_time`, `bruising`, `swelling`.

## Materials (owned by `materials.py`)

`GH_Skin` (subsurface, pores, redness/veins variation, `skin_tone`/`pallor`,
reads every gore attribute: wound → fat/muscle rings by `gore_depth`, blood,
bruise, burn, abrasion edge), `GH_Muscle`, `GH_Fat`, `GH_Bone` (reads
`gore_fracture`, `gore_blood`), `GH_Brain` (pinkish grey, wet, vessels), `GH_Blood`
(wet/dry by `blood_age`, `wetness`), `GH_Eye` (sclera, iris, pupil, glossy cornea,
bloodshot by `gore_blood`), `GH_Teeth`, `GH_Gums`, `GH_Tongue`, `GH_MouthInterior`,
`GH_Lips` optional. `assign_materials(objs, mats)` assigns them by object name.
